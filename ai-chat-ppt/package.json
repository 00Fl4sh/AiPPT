{
  "name": "ai-chat-ppt-root",
  "version": "1.0.0",
  "private": true,
  "description": "AI Chat PPT - Root package for Vercel deployment",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npm start",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
